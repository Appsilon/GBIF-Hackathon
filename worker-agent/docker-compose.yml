services:
  worker-agent:
    build:
      context: ..
      dockerfile: worker-agent/Dockerfile
    env_file: .env
    restart: unless-stopped
    volumes:
      - work-data:/work

volumes:
  work-data:
